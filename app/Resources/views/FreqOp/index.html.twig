{% extends 'base.html.twig' %}

{% block page_title -%}Liste des entités FreqOp{% endblock %}
{% block content -%}

        <p><a href="{{ path('operations-frequentes__new') }}">Créer une nouvelle entité</a></p>
    

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('operations-frequentes__show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.libelle }}</td>
                <td>
                <ul>
    <li>
        <a href="{{ path('operations-frequentes__show', { 'id': entity.id }) }}">
            Voir le détail
        </a>
    </li>
    <li>
        <a href="{{ path('operations-frequentes__edit', { 'id': entity.id }) }}">
            Modifier 
        </a>
    </li>

                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
