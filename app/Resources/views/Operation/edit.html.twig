{% extends 'base.html.twig' %}

{% block page_title -%}Modification de l'opération{% endblock %}
{% block content -%}
    
    {{ form_start(edit_form) }}
		{{ form_errors(edit_form) }}
		
		{{ form_widget(edit_form) }}
		
		<input type="submit" value="Modifier l'opération" />
	{{ form_end(edit_form) }}
	
	
        <ul class="record_actions">
    <li>
        <a href="{{ path('operations') }}">
            Retour à la liste des opérations
        </a>
    </li>
    <li>
		{{ form_start(delete_form) }}
		<input type="submit" value="Supprimer l'opération"/>
		{{ form_end(delete_form) }}
	</li>
</ul>
{% endblock %}
